#ifndef PROCESS_RING_H
#DEFINE PROCESS_RING_H

msg_process(pid32 *nextpid, int curr, pid32 *prev_pid, int32 count_val, int32 pro_cre);
A_proc(int *curr_ptr, int *next_ptr, int32 *count, int32 *total_rounds, int curr);


#ENDIF 
